// Copyright 2023 Furqan Software Ltd. All Rights Reserved.

@require './const'

html
	font-size 16px

body 
	font-family -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen-Sans, Ubuntu, 'Hind Siliguri', Cantarell, 'Helvetica Neue', sans-serif
	line-height 1.75em
	color $dark
	direction 'ltr'

h1,
h2,
h3,
h4
	font-weight normal

h1
	font-size 1.85em

h2
	font-size 1.5em

h3
	font-size 1.35em

h4
	font-size 1.2em

a
	text-decoration none
	color darken($blue, 19.5%) // Achieves 4.5 contrast ratio on white background.

	&:hover,
	&:focus
		text-decoration underline
		color darken($blue, 39%)
	
	&:active
		color darken($blue, 39%)

	&:focus-visible
		outline $blueglow auto 0.25rem
		outline-offset 0.25rem
	
	&:focus,
	&::-moz-focus-inner
		outline none
	
	&.-dark
		&,
		&:hover,
		&:focus,
		&:active
			color $dark
		

blockquote :last-child
	margin-bottom 0

summary
	font-weight 500
	cursor pointer
	
	.icon
		margin -0.25rem 0.75rem 0 0
		transition transform $fast ease-in
	
	[open] > &
		.icon
			transform rotate(90deg)

ul,
ol
	margin 0 0 0.5rem

ul ul,
ol ul,
ul ol,
ol ol
	margin-bottom 0

dt
	margin-bottom 0.25em
	font-weight 500

dd
	margin-left 0

blockquote
	margin 0 0 1.25rem
	padding 0.5rem 1.25rem
	border-left 0.25rem solid $silver

blockquote
	p,
	ul,
	ol
		&:last-child
			margin-bottom 0

hr
	margin 1.25rem 0
	border 0
	border-top 1px solid lighten($silver, 25%)

pre.-scroll
	overflow-y auto
	-webkit-overflow-scrolling touch
